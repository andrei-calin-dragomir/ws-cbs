events {}

http {
    server {
        listen 80;

        location /api/auth/ {
            proxy_pass http://auth_service:5001/;
        }

        location /api/shorten/ {
            proxy_pass http://url_shortener_service:5000/;
        }
    }
}
